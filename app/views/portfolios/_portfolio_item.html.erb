<div class="card" data-id="<%= portfolio_item.id %>">
  <%= image_tag portfolio_item.thumb_image, width: '300px' unless portfolio_item.thumb_image.nil? %>

  <p class="card-text">
    <span><%=link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></span><%# portfolio_show_path(portfolio_item) ==> because we are customising the routes, check the routes  %>
    <%= portfolio_item.subtitle %>
  </p>
</div>