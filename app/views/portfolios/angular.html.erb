<h1>Angular Portfolios#index</h1>

<%= link_to "Create New Portfolio", new_portfolio_url %>
<% @angular_portfolio_items.each do |portfolio_item| %>
  <h2><%=link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></h2>
  <%# portfolio_show_path(portfolio_item) ==> because we are customising the routes, check the routes  %>
  <p><%= portfolio_item.subtitle %></p>
  <p><%= portfolio_item.body %></p>
  <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
  <%= link_to "EDIT", edit_portfolio_path(portfolio_item) %>
  <%= link_to "DELETE", portfolio_path(portfolio_item), method: :delete, :data => { confirm: "are you sure?" } %>
<% end %>
